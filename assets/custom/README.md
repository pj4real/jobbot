Hand made resumes go here, named <job_id>.pdf

When the finder flags a posting as needing a manual pass it writes the job
description to data/jd/. Hand that file to a chat, get the LaTeX back, compile
it, and drop the pdf here as <job_id>.pdf. The mailer picks it up automatically
and never overwrites it.
